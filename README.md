# CMPS2212 – Lab #4: Event Delegation, closest(), and matches()

This lab introduces event delegation patterns
using event.target, closest(), and matches().

## Exercises

1. Accordion Menu (delegated click handler + closest)
2. Live Tag Builder (dynamic elements + matches)
3. Filterable Card Grid (delegation + dataset filtering)
4. Inline Edit List (dblclick delegation + edit mode switching)

## Concepts Learned

- Why delegation reduces multiple listeners
- How closest() safely traverses the DOM
- How matches() guards event logic
- Toggling CSS classes instead of inline styles
- Managing dynamic DOM elements cleanly

All exercises tested with zero console errors.

## Youtube Link:
